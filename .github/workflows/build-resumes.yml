name: Build Resume PDFs from LaTeX

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to checkout'
        required: true
        default: 'main'
      commit_id:
        description: 'Commit ID (SHA) to checkout (optional - uses latest if empty)'
        required: false
        default: ''

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: texlive/texlive:latest
    strategy:
      matrix:
        folder: [General, Backend, MLE, Infra, Experiment]
      fail-fast: false
    
    steps:
    - name: Determine checkout ref
      id: determine_ref
      run: |
        if [ -n "${{ github.event.inputs.commit_id }}" ]; then
          echo "ref=${{ github.event.inputs.commit_id }}" >> $GITHUB_OUTPUT
        else
          echo "ref=${{ github.event.inputs.branch }}" >> $GITHUB_OUTPUT
        fi
    
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        ref: ${{ steps.determine_ref.outputs.ref }}
    
    - name: Build ${{ matrix.folder }} resume
      run: |
        cd ${{ matrix.folder }}
        FOLDER_LOWER=$(echo "${{ matrix.folder }}" | tr '[:upper:]' '[:lower:]')
        mkdir -p .tmp ${FOLDER_LOWER}-resume-rendered-archive
        pdflatex -interaction=nonstopmode -output-directory=.tmp main.tex
        cp .tmp/main.pdf ${FOLDER_LOWER}-resume-rendered-archive/kausik-${FOLDER_LOWER}-$(date +%Y-%m-%d).pdf
        echo "PDF built in ${{ matrix.folder }}/${FOLDER_LOWER}-resume-rendered-archive/"
    
    - name: Upload ${{ matrix.folder }} resume artifact
      uses: actions/upload-artifact@v4
      with:
        name: kausik-${{ matrix.folder }}-resume
        path: ${{ matrix.folder }}/${{ matrix.folder }}-resume-rendered-archive/*.pdf

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts
        merge-multiple: true
    
    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        files: ./artifacts/*.pdf
        name: Resume PDFs - ${{ github.run_id }}
        tag_name: resumes-${{ github.run_id }}
        body: |
          Resume PDFs generated from commit ${{ github.sha }}
          
          ## Download Individual PDFs:
          - kausik-general-*.pdf
          - kausik-backend-*.pdf
          - kausik-mle-*.pdf
          - kausik-infra-*.pdf
          - kausik-experiment-*.pdf
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
